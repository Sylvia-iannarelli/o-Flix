<form method="post" action="{{ path('app_back_genre_delete', {'id': genre.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ genre.id) }}">
    <button class="btn btn-danger btn-lg"><i class="bi bi-trash"></i></button>
</form>
