<form method="post" action="{{ path('app_back_person_delete', {'id': person.id}) }}" onsubmit="return confirm('Confirmer la suppression ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ person.id) }}">
    <button class="btn btn-danger btn-lg"><i class="bi bi-trash"></i></button>
</form>
