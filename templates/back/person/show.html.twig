{% extends 'back/layout.html.twig' %}

{% block title %}Acteur{% endblock %}

{% block subBody %}
    <h1>Acteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ person.id }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ person.firstname }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ person.lastname }}</td>
            </tr>
        </tbody>
    </table>

    <div class="justify-content-center d-flex flex-nowrap gap-3">
        <a class="btn btn-success btn-lg" href="{{ path('app_back_person_index') }}"><i class="bi bi-box-arrow-left"></i></a>

        <a class="btn btn-warning btn-lg" href="{{ path('app_back_person_edit', {'id': person.id}) }}"><i class="bi bi-pencil"></i></a>

        {{ include('back/person/_delete_form.html.twig') }}
    </div>    

{% endblock %}
