{% extends 'back/layout.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block subBody %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Rôles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="justify-content-center d-flex flex-nowrap gap-3">
        <a class="btn btn-success btn-lg" href="{{ path('app_back_user_index') }}"><i class="bi bi-box-arrow-left"></i></a>

        <a class="btn btn-warning btn-lg" href="{{ path('app_back_user_edit', {'id': user.id}) }}"><i class="bi bi-pencil"></i></a>

        {{ include('back/user/_delete_form.html.twig') }}
    </div>    

{% endblock %}
